#include <stdio.h>

/**
* main - this is the main function
*
* Return: 0 if programs runs without errors
*/
int main(void)
{
	char str[] = "with proper grammar, but the outcome is a piece of art,";
	printf("%s\n", str);
	return (0);
}
